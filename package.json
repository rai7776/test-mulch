{
  "name": "smart-reader",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:foundation": "node --check smart-reader-foundation-core.js && node --check workspace-core.js && node --check workspace-ui.js && node --check note-structure-ui.js"
  }
}
