# Smart Reader 開発ルール

このリポジトリを編集するAI・開発者は、以下の運用ルールを守ること。

## セーブポイントを必ず作る

大きな機能追加、UIの全面変更、データ構造変更、既存機能の大幅な書き換え、複数ファイルにまたがる修正を始める前に、現在の安定した `main` を **Git tag のセーブポイント**として残す。

命名例:

```text
save/2026-09-09-before-flashcard-redesign
save/2026-09-09-before-note-structure-change
save/2026-09-09-stable
```

原則として `save/YYYY-MM-DD-before-<変更内容>` の形式を使い、何の変更前なのか人間が見て分かる名前にする。

### セーブポイントを作るべき変更

- 新機能の追加
- 既存機能の大幅な改修
- UI・レイアウトの大きな変更
- 保存データ・IndexedDB・localStorage・JSON形式などの変更
- 複数ファイルにまたがるリファクタリング
- 既存ユーザーのデータや学習履歴へ影響する可能性がある変更
- 後から「変更前へ戻したい」と思う可能性が高い変更

小さな文言修正、軽微なCSS調整、明確な単発バグ修正では毎回タグを増やす必要はない。

## branch運用

通常の開発branchは `feature/<name>` または `fix/<name>` とし、1つの変更目的につき原則1本だけ使う。`-work`、`-ready`、`-final`、`-impl` などの派生branchを大量に作らない。

開発が完了して `main` に反映されたbranchは削除してよい。ただし、重要な変更前の状態はbranchではなく上記の `save/...` tagで残す。

## セーブポイントを削除しない

`save/...` tagは、明示的な依頼がない限り削除しない。branch整理を行う場合も `save/...` tagは保護対象とする。

## 復元時

ユーザーが「○○を追加する前に戻したい」「前のUIに戻したい」などと依頼した場合は、まず `save/...` tagとcommit履歴を確認し、該当時点から復元用branchを作って比較する。いきなり `main` をforce resetしない。

例:

```text
restore/before-flashcard-redesign
```

復元内容を確認してから `main` へ反映する。

## 既存データの保護

Smart Readerでは既存ユーザーの学習データを最優先で保護する。保存形式や初期データを変更する場合は、既存データを上書き・削除しないマイグレーションを採用し、変更前に必ずセーブポイントを作る。

## 既存の履歴アーカイブ

2026-09-09以前のbranch整理時の履歴は `archive/branch-cleanup-2026-09-09` tagに保存されている。このtagも削除しないこと。
